- Search the web using the session's web search provider - performs real-time web searches and can scrape content from specific URLs
- Provides up-to-date information for current events and recent data
- Supports configurable result counts and returns the content from the most relevant websites
- Use this tool for accessing information beyond knowledge cutoff
- Searches are performed automatically within a single API call

Usage notes:
  - Search types: 'auto' (balanced), 'fast' (quick results), 'deep' (comprehensive search). Honoured by every provider
  - 'numResults' is honoured by every provider
  - 'livecrawl' and 'contextMaxCharacters' apply to Exa only and are ignored by other providers, so do not rely on them
  - The active provider is chosen by configuration, not per request; it cannot be selected from here

The current year is {{year}}. You MUST use this year when searching for recent information or current events
- Example: If the current year is 2026 and the user asks for "latest AI news", search for "AI news 2026", NOT "AI news 2025"
